jobs:
  - name: barman-cron
    command: barman cron
    schedule: "* * * * *"
  - name: metrics
    command: echo "would save metrics here"
    schedule: "*/5 * * * *"
  # this would be a good way of doing startup but we're stuck with
  # https://github.com/gjcarneiro/yacron/issues/22
  #
  # - name: startup
  #   command: echo "on startup"
  #   schedule: "@reboot"
